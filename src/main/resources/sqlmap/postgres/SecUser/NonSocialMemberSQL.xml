<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace=com.shoppingmall.admin.repository.MemberMapper -->
<mapper namespace="com.shoppingmall.secuser.MemberDao"> 
	<select id="getOneMemberByEmail" >
	  SELECT authid, loginid, email, password, nickname, role
	  FROM nonsocial_member
	  WHERE email =#{email}
	</select>	
	<select id="doCountLoginId" resultType="int">
		/*아이디 중복검사: SQL ID: MemberDao.doCountLoginId*/
		<![CDATA[
			SELECT
				COUNT(*)
			FROM
				nonsocial_member
			WHERE  = #{login_id}
		]]>
		
	</select>

</mapper>